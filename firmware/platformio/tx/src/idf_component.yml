## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=5.3.0'
  
  # OLED display driver
  k0i05/esp_ssd1306: '*'
  
  # USB Audio Class Device component (includes TinyUSB with proper descriptors)
  espressif/usb_device_uac: '^1.0.0'
